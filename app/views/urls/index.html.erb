<%= form_for @url do |u| %>
  <%= u.label :link %>
  <%= u.text_field :link %>
  <%= u.submit %>
<% end %>

<% if @urls %>
  <% @urls.each do |u| %>
    <%= link_to u.hashed, u.hashed %> =>
    <%= u.link %>
    <br>
  <% end %>
<% end %>
